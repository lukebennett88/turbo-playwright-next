{
  "name": "web-tests",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "test": "playwright test",
    "start-server": "cd ../../apps/web && npm run start",
    "e2e:test": "start-server-and-test start-server http://localhost:3000 test"
  },
  "dependencies": {
    "start-server-and-test": "^2.0.0",
    "web": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.33.0",
    "@types/node": "^17.0.12",
    "eslint": "^7.32.0",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*",
    "typescript": "^4.5.2"
  }
}
